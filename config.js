/**
 * Optional: set your production origin (no trailing slash). meta.js updates
 * og:url, og:image, twitter:image, and canonical when this is non-empty.
 *
 * For Facebook/Slack previews, also replace YOUR_PRODUCTION_URL in index.html,
 * sitemap.xml, and add a Sitemap line to robots.txt (see Netlify/host docs).
 *
 * Example: window.PIVOT_ORIGIN = "https://pivot.yourdomain.com";
 */
window.PIVOT_ORIGIN = "";
